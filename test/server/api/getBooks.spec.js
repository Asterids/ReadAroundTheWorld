const { expect } = require('chai');
const bookRouter = require('../../../server/api/getBooks')

describe('getBooks API route', () => {
  it('takes a location in plain English and returns a list of books', () => {
  })
});
